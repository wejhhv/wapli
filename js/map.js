//initの外に記述すると動かないので注意

document.addEventListener("init", (event) => {

  if(event.target.id === 'ons-map') {

    
    
  }

});

document.addEventListener("show", function(event) {

  if(event.target.id === 'Map') {
    
  }

});